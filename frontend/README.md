# Frontend for the chat application

## Getting started

Make sure to have `nodejs` installed and run `npm install` in the `frontend` directory from the command line.
After that succeeded, run `ng serve` from the command line, the development server should start up using `http://localhost:4200` in your browser.
